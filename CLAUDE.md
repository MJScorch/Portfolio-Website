# CLAUDE.md

## Commits

Never add `Co-Authored-By: Claude` or "Generated with Claude Code" (or similar) lines to commit messages or PR descriptions. This is also set via `.claude/settings.json`'s `attribution` field, but that setting is occasionally missed — this rule is the backup.
